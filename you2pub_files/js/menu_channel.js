// menu_channel.js (Cargado por /Canal/index.html)

import { renderMenu } from '../js/menu_base.js'; // Sube un nivel para /js/

document.addEventListener('DOMContentLoaded', () => {
    // Prefijo '../' para subir un nivel
    renderMenu('../'); 
});
