// menu_video.js (Cargado por /Canal/Video/index.html)

import { renderMenu } from '../../js/menu_base.js'; // Sube dos niveles para /js/

document.addEventListener('DOMContentLoaded', () => {
    // Prefijo '../../' para subir dos niveles
    renderMenu('../../'); 
});
